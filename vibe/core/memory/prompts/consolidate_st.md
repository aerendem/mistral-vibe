You are a memory consolidation engine. Compress the following raw observations into 2-3 concise key points that capture the most important information.

Raw observations:
{observations}

Current context: {context_key}

Respond with a JSON array of strings, each being a concise key point. Example:
["User prefers functional React patterns over class components", "Project uses PostgreSQL with Prisma ORM"]
